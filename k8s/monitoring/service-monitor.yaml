apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: devops-app-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: devops-app
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 15s
